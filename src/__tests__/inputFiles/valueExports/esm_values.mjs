export const VERSION = '2.0.0';    // ← 従来欠落
export let table = { k: 1 };       // ← 従来欠落
const NAMES = ['a'];
export { NAMES };                  // ← 従来欠落
export function build(x) { return x; }  // 関数
